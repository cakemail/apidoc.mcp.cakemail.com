// ABOUTME: Vitest configuration for the project.
// ABOUTME: Uses default settings with TypeScript support.

import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    globals: true,
  },
});
